# AI 智能写作系统设计方案

## 一、系统概述

### 1.1 功能定位
构建一个专业的 AI 智能写作系统，支持多种预设文档模板（政府公文、合同、申请表等），通过 AI 生成结构化数据，前端根据模板规则进行智能填充，提供类似在线编辑的完整体验。

### 1.2 核心特性
- 📝 **丰富的模板库**：预设多种专业文档模板
- 🤖 **AI 智能生成**：根据用户输入生成结构化内容
- ✏️ **在线编辑**：所见即所得的文档编辑体验
- 📄 **专业排版**：符合各类文档规范的格式输出
- 💾 **多格式导出**：支持 Word、PDF 等格式导出

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        AI 智能写作系统                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   模板选择    │ -> │   AI 生成    │ -> │   在线编辑    │      │
│  │   Template   │    │   Generate   │    │    Editor    │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│         │                   │                   │               │
│         v                   v                   v               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │  模板数据库   │    │  结构化数据   │    │   预览导出    │      │
│  │  Templates   │    │   JSON Data  │    │   Preview    │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、预设模板分类

### 3.1 政府公文类
| 模板名称 | 说明 | 字段结构 |
|---------|------|---------|
| 红头文件 | 标准政府红头文件格式 | 发文机关、文号、标题、主送机关、正文、附件、发文日期、抄送 |
| 通知 | 各类通知公告 | 标题、主送单位、正文、落款、日期 |
| 请示 | 向上级请示事项 | 标题、主送机关、请示事由、请示事项、结束语、落款 |
| 批复 | 对下级请示的答复 | 标题、主送机关、批复依据、批复意见、落款 |
| 报告 | 工作报告、调研报告 | 标题、报告对象、报告内容、结论建议、落款 |
| 函 | 平行机关往来公函 | 标题、主送机关、事由、请求/告知事项、落款 |
| 会议纪要 | 会议记录文档 | 会议名称、时间地点、参会人员、议题、决议事项 |

### 3.2 商务合同类
| 模板名称 | 说明 | 字段结构 |
|---------|------|---------|
| 采购合同 | 商品采购协议 | 甲乙方信息、采购清单、价格条款、交付条款、付款方式、违约责任 |
| 劳动合同 | 员工劳动协议 | 用人单位、劳动者信息、岗位、薪酬、工作时间、合同期限 |
| 租赁合同 | 房屋/设备租赁 | 出租方、承租方、租赁物、租金、租期、押金、维修责任 |
| 保密协议 | NDA 保密协议 | 双方信息、保密内容、保密期限、违约责任 |
| 合作协议 | 商业合作框架 | 合作方信息、合作内容、权利义务、利益分配、期限 |

### 3.3 申请表单类
| 模板名称 | 说明 | 字段结构 |
|---------|------|---------|
| 请假申请 | 员工请假单 | 申请人、部门、请假类型、起止时间、事由、审批人 |
| 报销申请 | 费用报销单 | 申请人、部门、报销项目、金额明细、附件说明 |
| 采购申请 | 物资采购申请 | 申请部门、采购物品、规格数量、预算、用途说明 |
| 出差申请 | 出差审批单 | 申请人、出差地点、时间、事由、预算 |
| 转正申请 | 员工转正申请 | 申请人、入职日期、部门岗位、工作总结、自我评价 |

### 3.4 日常办公类
| 模板名称 | 说明 | 字段结构 |
|---------|------|---------|
| 工作总结 | 周/月/年度总结 | 时间范围、工作概述、主要成果、问题不足、下步计划 |
| 工作计划 | 工作规划文档 | 时间范围、目标、具体任务、时间节点、资源需求 |
| 述职报告 | 个人述职文档 | 岗位职责、工作业绩、能力提升、存在问题、未来规划 |
| 项目方案 | 项目策划书 | 项目背景、目标、实施方案、时间计划、预算、风险 |

---

## 四、数据结构设计

### 4.1 模板定义结构

```typescript
interface DocumentTemplate {
  id: string;
  name: string;
  category: 'government' | 'contract' | 'application' | 'office';
  description: string;
  icon: string;
  thumbnail: string;
  
  // 模板字段定义
  fields: TemplateField[];
  
  // 排版样式配置
  style: TemplateStyle;
  
  // AI 生成提示词
  aiPrompt: string;
}

interface TemplateField {
  id: string;
  name: string;           // 字段名称
  label: string;          // 显示标签
  type: 'text' | 'textarea' | 'date' | 'select' | 'list' | 'table';
  required: boolean;
  placeholder?: string;
  options?: string[];     // select 类型的选项
  defaultValue?: string;
  validation?: {
    minLength?: number;
    maxLength?: number;
    pattern?: string;
  };
}

interface TemplateStyle {
  pageSize: 'A4' | 'Letter';
  margins: { top: number; right: number; bottom: number; left: number };
  titleStyle: TextStyle;
  subtitleStyle: TextStyle;
  bodyStyle: TextStyle;
  headerStyle?: HeaderStyle;  // 红头文件专用
}

interface TextStyle {
  fontFamily: string;
  fontSize: number;
  fontWeight: 'normal' | 'bold';
  color: string;
  lineHeight: number;
  textAlign: 'left' | 'center' | 'right' | 'justify';
  textIndent?: number;
}
```

### 4.2 AI 生成的结构化数据

```typescript
interface GeneratedDocument {
  templateId: string;
  generatedAt: number;
  
  // 结构化字段数据
  fieldValues: Record<string, FieldValue>;
  
  // 生成的完整内容（用于预览）
  renderedContent: string;
}

type FieldValue = string | string[] | TableData;

interface TableData {
  headers: string[];
  rows: string[][];
}
```

---

## 五、用户流程设计

### 5.1 完整使用流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户使用流程                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Step 1              Step 2              Step 3              Step 4    │
│  ┌─────────┐        ┌─────────┐        ┌─────────┐        ┌─────────┐  │
│  │ 选择模板 │   ->   │ 输入需求 │   ->   │ AI生成  │   ->   │ 在线编辑 │  │
│  │         │        │         │        │         │        │         │  │
│  │ 浏览分类 │        │ 描述场景 │        │ 流式输出 │        │ 实时预览 │  │
│  │ 预览样式 │        │ 关键信息 │        │ 结构化  │        │ 字段修改 │  │
│  └─────────┘        └─────────┘        └─────────┘        └─────────┘  │
│                                                                         │
│                                                     Step 5              │
│                                                   ┌─────────┐          │
│                                              ->   │ 导出下载 │          │
│                                                   │         │          │
│                                                   │ Word/PDF│          │
│                                                   └─────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 界面布局设计

#### 主界面 - 三栏布局

```
┌────────────────────────────────────────────────────────────────────────┐
│  AI 智能写作                                              [导出] [保存] │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌──────────────┐  ┌─────────────────────┐  ┌───────────────────────┐ │
│  │              │  │                     │  │                       │ │
│  │   模板列表    │  │     编辑区域        │  │      实时预览         │ │
│  │              │  │                     │  │                       │ │
│  │  ┌────────┐  │  │  ┌───────────────┐  │  │  ┌─────────────────┐  │ │
│  │  │政府公文│  │  │  │ 文档标题      │  │  │  │                 │  │ │
│  │  ├────────┤  │  │  └───────────────┘  │  │  │   红头文件      │  │ │
│  │  │ 红头   │  │  │  ┌───────────────┐  │  │  │                 │  │ │
│  │  │ 通知   │  │  │  │ 发文机关      │  │  │  │   XX市人民政府  │  │ │
│  │  │ 请示   │  │  │  └───────────────┘  │  │  │                 │  │ │
│  │  │ 批复   │  │  │  ┌───────────────┐  │  │  │   关于XXX的通知 │  │ │
│  │  └────────┘  │  │  │ 正文内容      │  │  │  │                 │  │ │
│  │              │  │  │               │  │  │  │   各单位：      │  │ │
│  │  ┌────────┐  │  │  │               │  │  │  │   ............  │  │ │
│  │  │商务合同│  │  │  │               │  │  │  │                 │  │ │
│  │  ├────────┤  │  │  └───────────────┘  │  │  │                 │  │ │
│  │  │ 采购   │  │  │                     │  │  └─────────────────┘  │ │
│  │  │ 劳动   │  │  │  [AI 智能填充]      │  │                       │ │
│  │  └────────┘  │  │                     │  │                       │ │
│  │              │  │                     │  │                       │ │
│  └──────────────┘  └─────────────────────┘  └───────────────────────┘ │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 六、核心功能实现

### 6.1 AI 智能生成

#### 生成流程
1. 用户选择模板后，显示 AI 输入框
2. 用户输入需求描述（如："帮我写一份关于年终奖发放的通知"）
3. 系统构建 prompt，包含模板结构和用户需求
4. 调用 AI API，流式返回结构化 JSON 数据
5. 前端解析 JSON，填充到对应字段

#### AI Prompt 模板示例

```
你是一个专业的公文写作助手。请根据用户需求，生成符合以下模板结构的内容。

模板类型：{templateName}
模板字段：
{fieldDefinitions}

用户需求：{userInput}

请以 JSON 格式输出，格式如下：
{
  "fields": {
    "title": "文档标题",
    "recipient": "主送单位",
    "content": "正文内容...",
    ...
  }
}

要求：
1. 内容专业、规范，符合{templateName}的写作规范
2. 语言正式、准确，避免口语化表达
3. 结构清晰，逻辑严谨
4. 所有必填字段都要填写完整
```

### 6.2 在线编辑器

#### 编辑器功能
- **字段编辑**：点击字段可直接编辑
- **富文本支持**：正文区域支持基础格式（加粗、列表等）
- **实时预览**：编辑内容实时同步到预览区
- **撤销重做**：支持操作历史
- **自动保存**：定时保存到本地存储

#### 编辑器组件结构

```typescript
interface EditorProps {
  template: DocumentTemplate;
  initialData?: GeneratedDocument;
  onChange: (data: GeneratedDocument) => void;
}

// 字段编辑器 - 根据字段类型渲染不同组件
const FieldEditor: React.FC<{
  field: TemplateField;
  value: FieldValue;
  onChange: (value: FieldValue) => void;
}>;

// 预览渲染器 - 根据模板样式渲染文档
const DocumentPreview: React.FC<{
  template: DocumentTemplate;
  data: GeneratedDocument;
}>;
```

### 6.3 文档预览与导出

#### 预览功能
- A4 纸张比例显示
- 支持缩放（50%、75%、100%、150%）
- 分页预览
- 打印预览模式

#### 导出功能
- **Word 导出**：使用 docx 库生成 .docx 文件
- **PDF 导出**：使用 html2pdf 或 jsPDF 生成 PDF
- **HTML 导出**：导出可打印的 HTML 文件

---

## 七、UI 设计规范

### 7.1 配色方案

```css
/* 主色调 */
--primary: #2563eb;        /* 蓝色 - 主要操作 */
--primary-light: #dbeafe;  /* 浅蓝 - 背景 */

/* 分类色彩 */
--government: #dc2626;     /* 红色 - 政府公文 */
--contract: #059669;       /* 绿色 - 商务合同 */
--application: #d97706;    /* 橙色 - 申请表单 */
--office: #7c3aed;         /* 紫色 - 日常办公 */

/* 中性色 */
--text-primary: #111827;
--text-secondary: #6b7280;
--border: #e5e7eb;
--background: #f9fafb;
```

### 7.2 模板卡片设计

```
┌─────────────────────────────┐
│  ┌───────────────────────┐  │
│  │                       │  │
│  │     📄 缩略图预览      │  │
│  │                       │  │
│  └───────────────────────┘  │
│                             │
│  红头文件                    │
│  标准政府红头文件格式         │
│                             │
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ 公文 │ │ 正式 │ │ 常用 │   │
│  └─────┘ └─────┘ └─────┘   │
│                             │
│  [使用此模板]               │
└─────────────────────────────┘
```

### 7.3 文档预览样式

#### 红头文件样式
```css
.red-header-doc {
  /* 红头 */
  .header-line {
    border-top: 2px solid #dc2626;
    border-bottom: 2px solid #dc2626;
    padding: 8px 0;
    text-align: center;
  }
  
  .org-name {
    font-size: 22pt;
    font-weight: bold;
    color: #dc2626;
    letter-spacing: 4px;
  }
  
  /* 文号 */
  .doc-number {
    text-align: center;
    font-size: 14pt;
    margin: 16px 0;
  }
  
  /* 标题 */
  .doc-title {
    font-size: 22pt;
    font-weight: bold;
    text-align: center;
    margin: 24px 0;
  }
  
  /* 正文 */
  .doc-body {
    font-size: 16pt;
    line-height: 2;
    text-indent: 2em;
  }
}
```

---

## 八、技术实现要点

### 8.1 组件结构

```
src/
├── pages/
│   └── AIWritingPage.tsx          # 主页面
├── components/
│   └── writing/
│       ├── TemplateSelector.tsx    # 模板选择器
│       ├── TemplateCard.tsx        # 模板卡片
│       ├── AIInputPanel.tsx        # AI 输入面板
│       ├── DocumentEditor.tsx      # 文档编辑器
│       ├── FieldEditor.tsx         # 字段编辑器
│       ├── DocumentPreview.tsx     # 文档预览
│       ├── ExportPanel.tsx         # 导出面板
│       └── templates/              # 各模板预览组件
│           ├── RedHeaderDoc.tsx
│           ├── ContractDoc.tsx
│           └── ApplicationForm.tsx
├── data/
│   └── templates/
│       ├── government.ts           # 政府公文模板
│       ├── contracts.ts            # 合同模板
│       ├── applications.ts         # 申请表模板
│       └── office.ts               # 办公文档模板
└── types/
    └── writing.ts                  # 类型定义
```

### 8.2 状态管理

```typescript
interface WritingState {
  // 当前选中的模板
  selectedTemplate: DocumentTemplate | null;
  
  // 当前文档数据
  documentData: GeneratedDocument | null;
  
  // 编辑历史（用于撤销/重做）
  history: GeneratedDocument[];
  historyIndex: number;
  
  // UI 状态
  isGenerating: boolean;
  previewScale: number;
  showExportPanel: boolean;
}
```

### 8.3 关键依赖

```json
{
  "dependencies": {
    "docx": "^8.0.0",           // Word 文档生成
    "html2pdf.js": "^0.10.1",   // PDF 导出
    "react-contenteditable": "^3.3.7"  // 可编辑内容
  }
}
```

---

## 九、开发计划

### Phase 1: 基础框架（核心）
1. 创建 AIWritingPage 主页面
2. 实现模板数据结构和预设模板（至少 8 个）
3. 实现模板选择界面
4. 实现 AI 生成功能（流式输出）

### Phase 2: 编辑功能
5. 实现文档编辑器组件
6. 实现各类型字段编辑器
7. 实现实时预览功能

### Phase 3: 导出与优化
8. 实现 Word/PDF 导出
9. 优化 UI 交互和动画
10. 添加本地存储和历史记录

---

## 十、示例模板详细设计

### 10.1 红头文件模板

```typescript
const redHeaderTemplate: DocumentTemplate = {
  id: 'red-header',
  name: '红头文件',
  category: 'government',
  description: '标准政府红头文件格式，适用于正式公文发布',
  icon: '📜',
  thumbnail: 'red-header-preview',
  
  fields: [
    {
      id: 'orgName',
      name: 'orgName',
      label: '发文机关',
      type: 'text',
      required: true,
      placeholder: '如：XX市人民政府',
    },
    {
      id: 'docNumber',
      name: 'docNumber',
      label: '发文字号',
      type: 'text',
      required: true,
      placeholder: '如：X政发〔2024〕1号',
    },
    {
      id: 'title',
      name: 'title',
      label: '文件标题',
      type: 'text',
      required: true,
      placeholder: '关于XXX的通知/决定/意见',
    },
    {
      id: 'recipient',
      name: 'recipient',
      label: '主送机关',
      type: 'text',
      required: true,
      placeholder: '各区县人民政府，市政府各部门：',
    },
    {
      id: 'content',
      name: 'content',
      label: '正文内容',
      type: 'textarea',
      required: true,
      placeholder: '请输入正文内容...',
    },
    {
      id: 'attachment',
      name: 'attachment',
      label: '附件',
      type: 'text',
      required: false,
      placeholder: '附件名称（如有）',
    },
    {
      id: 'issueDate',
      name: 'issueDate',
      label: '发文日期',
      type: 'date',
      required: true,
    },
    {
      id: 'cc',
      name: 'cc',
      label: '抄送',
      type: 'text',
      required: false,
      placeholder: '抄送单位（如有）',
    },
  ],
  
  style: {
    pageSize: 'A4',
    margins: { top: 37, right: 26, bottom: 35, left: 28 },
    titleStyle: {
      fontFamily: '方正小标宋简体',
      fontSize: 22,
      fontWeight: 'bold',
      color: '#000000',
      lineHeight: 1.5,
      textAlign: 'center',
    },
    subtitleStyle: {
      fontFamily: '黑体',
      fontSize: 16,
      fontWeight: 'bold',
      color: '#000000',
      lineHeight: 1.5,
      textAlign: 'left',
    },
    bodyStyle: {
      fontFamily: '仿宋',
      fontSize: 16,
      fontWeight: 'normal',
      color: '#000000',
      lineHeight: 2,
      textAlign: 'justify',
      textIndent: 2,
    },
    headerStyle: {
      color: '#dc2626',
      borderWidth: 2,
    },
  },
  
  aiPrompt: `你是一个专业的政府公文写作助手。请根据用户需求生成标准的红头文件内容。
要求：
1. 语言正式、规范，符合公文写作标准
2. 结构完整，包含发文机关、文号、标题、主送机关、正文、落款等要素
3. 正文层次分明，可使用"一、二、三"或"（一）（二）（三）"等序号
4. 结尾使用规范的公文结束语`,
};
```

---

以上是完整的 AI 智能写作系统设计方案。是否需要我进一步细化某个部分，或者开始进入代码实现阶段？